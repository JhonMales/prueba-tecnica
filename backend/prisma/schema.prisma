generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Proyecto {
  id           Int      @id @default(autoincrement())
  nombre       String
  descripcion  String
  estado       String
  fechaInicio  DateTime
  fechaFin     DateTime
}
